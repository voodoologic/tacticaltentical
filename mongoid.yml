production:
  sessions:
    default:
      hosts:
        - 'localhost:27017'
      # uri: <%= ENV['MONGOHQ_URL'] || 'localhost'%>
      database: 'mongoid'
      options:
        skip_version_check: true
        safe: true
development:
  :sessions:
    :default:
      database: 'mongoid'
      hosts:
      - 'localhost:27017'
      :options:
